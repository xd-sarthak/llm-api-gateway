apiVersion: 1

providers:
  - name: llm-gateway
    orgId: 1
    folder: LLM Gateway
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
